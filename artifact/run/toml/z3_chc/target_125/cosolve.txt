marker          : full_date_first_dash_wrong_char
raised in       : parse_full_date
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 9 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (1730 ms) ===
--- reply ---
<<<
2024/01-01
>>>
candidate: "2024/01-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR key_value_missing_equals_char instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 2 (2865 ms) ===
--- reply ---
input <<<
a = 2024/01-01
>>>
candidate: "a = 2024/01-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 3 (3217 ms) ===
--- reply ---
<<<a = 2024-a1-01>>>
candidate: "a = 2024-a1-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR date_month_first_char_not_digit instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 4 (0 ms) ===
--- reply ---
<<<
a = 2024/01-01
>>>
candidate: "a = 2024/01-01"
--- outcome ---
that candidate was already rejected — propose a DIFFERENT one

=== round 5 (1519 ms) ===
--- reply ---
<<<
a = 2024x01-01
>>>
candidate: "a = 2024x01-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 6 (2739 ms) ===
--- reply ---
<<<
a = [2024/01-01]
>>>
candidate: "a = [2024/01-01]"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR array_value_invalid_separator instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 7 (0 ms) ===
--- reply ---
<<<a = 2024/01-01>>>
candidate: "a = 2024/01-01"
--- outcome ---
that candidate was already rejected — propose a DIFFERENT one

=== round 8 (2043 ms) ===
--- reply ---
<<<
a = 2024−01-01
>>>
candidate: "a = 2024−01-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

=== round 9 (1512 ms) ===
--- reply ---
<<<a = 2024a01-01>>>
candidate: "a = 2024a01-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_wrong_char`. Z3 reports that it reaches ERR integer_dec_hex_char_in_body instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_wrong_char` is about.

status: no witness (stopped: budget)
