marker          : full_date_first_dash_without_second
raised in       : parse_full_date
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 9 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (2721 ms) ===
--- reply ---
<<<
1979-
>>>
candidate: "1979-"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR key_value_missing_equals_eof instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 2 (1457 ms) ===
--- reply ---
<<<a = 1979->>>
candidate: "a = 1979-"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 3 (2031 ms) ===
--- reply ---
<<<
a = 1979-01
>>>
candidate: "a = 1979-01"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 4 (2361 ms) ===
--- reply ---
<<<a = 1979-0>>>
candidate: "a = 1979-0"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 5 (2047 ms) ===
--- reply ---
<<<a = [1979-05]>>>
candidate: "a = [1979-05]"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR array_value_invalid_separator instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 6 (2382 ms) ===
--- reply ---
<<<[1979-05-]>>>
candidate: "[1979-05-]"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports OK for it — the document is simply valid, so it violates no rule at all. It has to break exactly the rule `full_date_first_dash_without_second` names.

=== round 7 (2797 ms) ===
--- reply ---
<<<
a = 1979-05-
>>>
candidate: "a = 1979-05-"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR full_date_expect_mday_after_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 8 (1509 ms) ===
--- reply ---
<<<a = 1979-05x>>>
candidate: "a = 1979-05x"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

=== round 9 (1467 ms) ===
--- reply ---
<<<a = 1979-05>>>
candidate: "a = 1979-05"
--- outcome ---
`unsat`: this input does not reach `full_date_first_dash_without_second`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_first_dash_without_second` is about.

status: no witness (stopped: budget)
