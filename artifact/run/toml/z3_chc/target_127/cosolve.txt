marker          : full_date_second_dash_wrong_char
raised in       : parse_full_date
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 9 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (1977 ms) ===
--- reply ---
<<<
x = 1979-05/27
>>>
candidate: "x = 1979-05/27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 2 (1502 ms) ===
--- reply ---
<<<
x = 1979-05X27
>>>
candidate: "x = 1979-05X27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 3 (1841 ms) ===
--- reply ---
<<<1979-05x27>>>
candidate: "1979-05x27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR key_value_missing_equals_eof instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 4 (2184 ms) ===
--- reply ---
<<<x=1979-05/27>>>
candidate: "x=1979-05/27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 5 (0 ms) ===
--- reply ---
<<<x = 1979-05/27>>>
candidate: "x = 1979-05/27"
--- outcome ---
that candidate was already rejected — propose a DIFFERENT one

=== round 6 (2386 ms) ===
--- reply ---
<<<
x = 1979-05a27
>>>
candidate: "x = 1979-05a27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 7 (2228 ms) ===
--- reply ---
<<<x = 1979-05-a>>>
candidate: "x = 1979-05-a"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR date_mday_first_char_not_digit instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 8 (1569 ms) ===
--- reply ---
<<<
x = 1979-05?27
>>>
candidate: "x = 1979-05?27"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

=== round 9 (1511 ms) ===
--- reply ---
<<<x = 1979-05\u{0}>>>
candidate: "x = 1979-05\0"
--- outcome ---
`unsat`: this input does not reach `full_date_second_dash_wrong_char`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_second_dash_wrong_char` is about.

status: no witness (stopped: budget)
