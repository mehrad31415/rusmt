marker          : date_year_first_char_not_digit
raised in       : parse_date_fullyear
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 9 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (1465 ms) ===
--- reply ---
input <<<
a
>>>
candidate: "a"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR key_value_missing_equals_eof instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 2 (1931 ms) ===
--- reply ---
<<<
a = a
>>>
candidate: "a = a"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR key_value_missing_value_nomatch instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 3 (1616 ms) ===
--- reply ---
<<<
a = -0001-01-01
>>>
candidate: "a = -0001-01-01"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR integer_dec_leading_zero_digit instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 4 (1572 ms) ===
--- reply ---
<<<a = +1234-01-01>>>
candidate: "a = +1234-01-01"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 5 (1584 ms) ===
--- reply ---
<<<
a = +a234-01-01
>>>
candidate: "a = +a234-01-01"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR integer_dec_plus_sign_no_digits_other instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 6 (1860 ms) ===
--- reply ---
<<<a = +a234-01-01T00:00:00>>>
candidate: "a = +a234-01-01T00:00:00"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR integer_dec_plus_sign_no_digits_other instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 7 (1523 ms) ===
--- reply ---
<<<
a = x1979-01-01
>>>
candidate: "a = x1979-01-01"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR key_value_missing_value_nomatch instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 8 (1639 ms) ===
--- reply ---
<<<
a = 1979-01-01T00:00:00x
>>>
candidate: "a = 1979-01-01T00:00:00x"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

=== round 9 (1629 ms) ===
--- reply ---
<<<
a = 1x79-01-01
>>>
candidate: "a = 1x79-01-01"
--- outcome ---
`unsat`: this input does not reach `date_year_first_char_not_digit`. Z3 reports that it reaches ERR date_year_second_char_not_digit instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `date_year_first_char_not_digit` is about.

status: no witness (stopped: budget)
