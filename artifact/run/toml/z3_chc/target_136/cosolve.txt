marker          : full_date_expect_mday_after_month
raised in       : parse_full_date
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 3 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (1465 ms) ===
--- reply ---
<<<1979-05>>>
candidate: "1979-05"
--- outcome ---
`unsat`: this input does not reach `full_date_expect_mday_after_month`. Z3 reports that it reaches ERR key_value_missing_equals_eof instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_expect_mday_after_month` is about.

=== round 2 (1999 ms) ===
--- reply ---
<<<a = 1979-05>>>
candidate: "a = 1979-05"
--- outcome ---
`unsat`: this input does not reach `full_date_expect_mday_after_month`. Z3 reports that it reaches ERR toml_expected_newline_between_expressions instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `full_date_expect_mday_after_month` is about.

=== round 3 (758 ms) ===
--- reply ---
<<<
a = 1979-05-
>>>
candidate: "a = 1979-05-"
--- outcome ---
ACCEPTED: Z3 certified this input against the unmodified query (sat) — a witness for `full_date_expect_mday_after_month`

status: 1 witness(es) accepted:
  "a = 1979-05-"
