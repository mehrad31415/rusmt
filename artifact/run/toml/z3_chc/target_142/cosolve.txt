marker          : time_hour_first_char_not_digit
raised in       : parse_time_hour
stage 1 (Z3 on the unmodified query): timeout
proposer        : command proposer `sandbox-exec -f /tmp/rusmt-run/no-source.sb codex exec -m gpt-5.4-mini -c model_reasoning_effort="low" -s read-only --skip-git-repo-check -o last.txt >/dev/null 2>&1; cat last.txt` (cache /tmp/rusmt-cache-final, 0 hit / 9 invoked)

The proposer reads the emitted SMT-LIB and returns one candidate. Every witness below
is a model of the UNMODIFIED query, obtained by pinning the candidate into it and
re-solving; a rejected candidate is a counterexample fed into the next round.

=== round 1 (1563 ms) ===
--- reply ---
<<<
1979-05-27T:00:00
>>>
candidate: "1979-05-27T:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR key_value_missing_equals_char instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 2 (1570 ms) ===
--- reply ---
<<<
a = 1979-05-27T:00:00
>>>
candidate: "a = 1979-05-27T:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 3 (1514 ms) ===
--- reply ---
<<<
a = :00:00
>>>
candidate: "a = :00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR key_value_missing_value_nomatch instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 4 (1629 ms) ===
--- reply ---
<<<
a = 1979-01-01T:00:00
>>>
candidate: "a = 1979-01-01T:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 5 (1593 ms) ===
--- reply ---
<<<a = 1979-05-27Ta:00:00>>>
candidate: "a = 1979-05-27Ta:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 6 (1639 ms) ===
--- reply ---
<<<
a = 1979-05-27T a:00:00
>>>
candidate: "a = 1979-05-27T a:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 7 (1585 ms) ===
--- reply ---
<<<a = 1979-05-27 a:00:00>>>
candidate: "a = 1979-05-27 a:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 8 (1675 ms) ===
--- reply ---
<<<a = 1979-05-27ta:00:00>>>
candidate: "a = 1979-05-27ta:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

=== round 9 (1854 ms) ===
--- reply ---
<<<
a = 1979-05-27Tn0:00:00
>>>
candidate: "a = 1979-05-27Tn0:00:00"
--- outcome ---
`unsat`: this input does not reach `time_hour_first_char_not_digit`. Z3 reports that it reaches ERR date_invalid_month instead — a DIFFERENT rule fired first and shadowed the one you want. Fix that earlier violation so the document is valid up to the point `time_hour_first_char_not_digit` is about.

status: no witness (stopped: budget)
