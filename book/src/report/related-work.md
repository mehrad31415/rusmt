## Related work

Rusmart sits at the intersection of:

- **semantic testing / differential testing** (using a reference semantics to test implementations),
- **symbolic execution / constraint solving** (encoding executions as constraints),
- **SMT-based synthesis** (asking solvers to produce inputs/models).

This chapter is a placeholder for the proposal’s bibliography and positioning; the concrete implementation details live in the developer chapters.

