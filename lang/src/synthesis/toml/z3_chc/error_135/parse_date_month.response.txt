sat: model found
 sat
(
  (define-fun input_parse_date_month_0 () State
    (mk-State (mk-ParserContext (seq.unit (seq.unit "!0!"))
                            (seq.unit (seq.unit "!0!"))
                            (as seq.empty (Seq String))
                            (seq.unit (seq.unit "!0!"))
                            (seq.unit (seq.unit "!0!"))
                            (seq.unit (seq.unit "!0!")))
          (- 1)
          (as seq.empty (Seq String))))
  (define-fun default_parser_context () ParserContext
    (mk-ParserContext (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq String))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))))
  (define-fun array_null_Optional_Array_String_Value () (Optional (Array String Value))
    (as None (Optional (Array String Value))))
  (define-fun array_null_Optional_String () (Optional String)
    (as None (Optional String)))
  (define-fun array_null_KeyVal () KeyVal
    (mk-KeyVal (as seq.empty (Seq String)) (String "")))
  (define-fun array_null_Number () Number
    (F64 (_ NaN 11 53)))
  (define-fun array_null_ParseResult___BitVec_64 () (ParseResult (_ BitVec 64))
    (as NoMatch (ParseResult (_ BitVec 64))))
  (define-fun array_null_ParseResult_Value () (ParseResult Value)
    (as NoMatch (ParseResult Value)))
  (define-fun array_null_ParseResult_Number () (ParseResult Number)
    (as NoMatch (ParseResult Number)))
  (define-fun array_null_ParseResult_Bool () (ParseResult Bool)
    (as NoMatch (ParseResult Bool)))
  (define-fun array_null_State () State
    (mk-State (mk-ParserContext (as seq.empty (Seq (Seq String)))
                            (as seq.empty (Seq (Seq String)))
                            (as seq.empty (Seq String))
                            (as seq.empty (Seq (Seq String)))
                            (as seq.empty (Seq (Seq String)))
                            (as seq.empty (Seq (Seq String))))
          0
          (as seq.empty (Seq String))))
  (define-fun array_null_ParseResult_String () (ParseResult String)
    (as NoMatch (ParseResult String)))
  (define-fun array_null_ParseResult_Seq_Value () (ParseResult (Seq Value))
    (as NoMatch (ParseResult (Seq Value))))
  (define-fun array_null_ParseResult_DateTime () (ParseResult DateTime)
    (as NoMatch (ParseResult DateTime)))
  (define-fun array_null_ParseResult_Int () (ParseResult Int)
    (as NoMatch (ParseResult Int)))
  (define-fun array_null_ParseResult___FloatingPoint_11_53 () (ParseResult (_ FloatingPoint 11 53))
    (as NoMatch (ParseResult (_ FloatingPoint 11 53))))
  (define-fun array_null_DateTime () DateTime
    (LocalDate ""))
  (define-fun array_null_ParseResult_Array_String_Value () (ParseResult (Array String Value))
    (as NoMatch (ParseResult (Array String Value))))
  (define-fun array_null_ParseResult_Seq_String () (ParseResult (Seq String))
    (as NoMatch (ParseResult (Seq String))))
  (define-fun array_null_ParserContext () ParserContext
    (mk-ParserContext (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq String))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))
                  (as seq.empty (Seq (Seq String)))))
  (define-fun array_null_Value () Value
    (String ""))
  (define-fun array_null_ParseResult_KeyVal () (ParseResult KeyVal)
    (as NoMatch (ParseResult KeyVal)))
  (define-fun array_key_min ((x!0 (Array String Value))) String
    "")
  (define-fun seq.nth_u ((x!0 (Seq String)) (x!1 Int)) String
    (ite (and (= x!0 (as seq.empty (Seq String))) (= x!1 0))
      "\u{20000}\u{20000}"
      "\u{20000}"))
)
