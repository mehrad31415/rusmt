



we have an implementation:

#[smt_impl]
fn add(lhs: Point, rhs: Point) -> Point {
    Point::new(lhs.x + rhs.x, lhs.y + rhs.y)
}


#[smt_spec]
fn add_spec(lhs: Point, rhs: Point) -> Point {
....
}

#[smt_axiom]
fn add_axiom(lhs: Point, rhs: Point) -> Boolean {
....
}



------------





what is a relevant axiom:

relation: spec and impl
we get the spec, impl, and all function calls as set X
for each of them there is a set of axioms: what is that axiom set?
we get all the user function calls for each axiom as set Y
if Y is a subset of X, then we write that axiom.